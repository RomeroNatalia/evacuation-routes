# Source Package

The reusable Python package is in `src/fire_evacuation/`. Scripts add `src/` to the Python path directly, and installing the project with `pip install -e .` also makes the package importable.
