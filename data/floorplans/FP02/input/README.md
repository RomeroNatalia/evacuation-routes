# Input data

These five standardized input files are the manually prepared source dataset. Do not edit generated files in `../output/` as substitutes for these inputs.

## FP02 geometry rule

All legal graph edges in FP02 are horizontal or vertical in grid coordinates. Diagonal source-target connections are not permitted. Door and exit transitions must connect to aligned room or hallway nodes.

