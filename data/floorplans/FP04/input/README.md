# Input data

These five standardized input files are the manually prepared source dataset. Do not edit generated files in `../output/` as substitutes for these inputs.

## Confirmed graph corrections

The current FP04 source graph includes the reviewed corrections for `START_R05`, `START_R06`, and the East Gallery C nodes at `Q13` and `Q15`. Generated output files were rebuilt from these inputs after validation.
