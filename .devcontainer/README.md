# Development Container

`devcontainer.json` creates a Python 3.11 Codespaces/container environment and installs the project from `pyproject.toml`. D-Wave credentials are not stored here and must be configured separately by each user.
