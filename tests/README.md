# Tests

Run from the repository root:

```bash
pytest
```

The parameterized tests cover FP01 through FP05. They validate the input tables, room/occupancy agreement, doorway degree, room-to-exit reachability, route counts, and A*/Dijkstra distance agreement.
